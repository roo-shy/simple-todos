# simple-todos

#Installation

###Create an app

`meteor create simple-todos`

###Run an app

`cd simple-todos`  
`meteor npm install`  
`meteor`

###Run an app on IOS simulator

`meteor install-sdk ios`
`meteor add-platform ios`
